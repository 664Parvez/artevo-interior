export const metadata = {
    title: "Home Automation | Artevo Interior",
    description: "It is agency of Student Counciling",
};

export default function RootLayout({ children }) {
    return (
        <>
            {children}
        </>
    )
}